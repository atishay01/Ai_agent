# Secrets
.env

# Python
__pycache__/
*.pyc
*.pyo
.venv/

# Logs (structured JSON from loguru)
logs/

# Tooling caches
.ruff_cache/
.mypy_cache/
.pytest_cache/
.coverage

# Data (too large for git)
data/raw/*.csv

# Persistent state (SQLite store: session memory + response cache)
data/state.db
data/state.db-shm
data/state.db-wal

# Local helpers (not shared)
run.bat
stop.bat

# OS
Thumbs.db
.DS_Store
